# 🏠 ng-house-hunters

Angular tutorial project: Building a housing location finder app with standalone components, routing, and forms.

Based on the [official Angular tutorial](https://angular.io/tutorial/first-app) from the Angular team.

## 🚀 Getting Started

### Prerequisites

- Node.js (v16 or higher)
- npm (comes with Node.js)

### Installation

1. **Clone this repository**

   ```bash
   git clone https://github.com/sbogde/ng-house-hunters.git
   cd ng-house-hunters
   ```

2. **Install dependencies**

   ```bash
   npm install
   ```

3. **Run the development server**

   ```bash
   npm start
   ```

   The app will automatically open at `http://localhost:4200/`

## 🛠️ Available Commands

- `npm start` - Start the development server
- `npm run build` - Build the project for production
- `npm run watch` - Build in watch mode
- `npm test` - Run unit tests
- `npx ng generate component <name>` - Generate a new component

## 📚 What You'll Learn

- Standalone components (modern Angular approach)
- Component communication with `@Input()` and `@Output()`
- Routing and navigation
- Template-driven and reactive forms
- Services and dependency injection
- Angular CLI usage

## 🔧 Tech Stack

- **Angular 16** - Frontend framework
- **TypeScript** - Type-safe JavaScript
- **RxJS** - Reactive programming

## 📝 Notes

This project uses Angular 16 locally. If you have a different global Angular CLI version, use `npx ng` commands to ensure you're using the project's local version:

```bash
npx ng serve
npx ng generate component my-component
```

## 📖 Original Tutorial

This project follows the official Angular tutorial from the [Angular codelabs repository](https://github.com/angular/codelabs).

## 📄 License

MIT